import React from "react";
import { Text, View } from "react-native";
import { globleStyle } from "../styles/global";

const Home = () => (
  <View style={globleStyle.container}>
    <Text style={globleStyle.titleText}>Home</Text>
  </View>
);

export default Home;
